name: Chrome Extension Tests

on:
  push:
    branches: ["master"]
  workflow_dispatch:  # Allow manual triggers

jobs:
  test:
    name: Run Playwright Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set template URL from repo file
        run: |
          # Get the absolute path to template0.html
          TEMPLATE_PATH="${GITHUB_WORKSPACE}/template0.html"
          
          # Convert to file:// URL for browser
          TEMPLATE_URL="file://${TEMPLATE_PATH}"
          
          echo "TEMPLATE_URL=${TEMPLATE_URL}" >> $GITHUB_ENV
          echo "Template URL set to: ${TEMPLATE_URL}"
          
          # Verify file exists
          if [ -f "${TEMPLATE_PATH}" ]; then
            echo "✓ template0.html found in repository"
          else
            echo "✗ ERROR: template0.html not found!"
            echo "Please ensure template0.html is in the root of your repository"
            exit 1
          fi
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Install Playwright browsers
        run: |
          playwright install chromium
          playwright install-deps chromium
      
      # Create test results directories before running tests
      - name: Create test results directories
        run: |
          mkdir -p test-results/screenshots
          mkdir -p test-results/traces
          echo "Test results directories created"
      
      # IMPORTANT: This is where you specify your Chrome extension
      
      # === OPTION 1: Download ZIP from URL (RECOMMENDED - Fastest!) ===
      - name: Download Chrome Extension ZIP
        run: |
          # Add cache-busting timestamp to force fresh download
          TIMESTAMP=$(date +%s)
          echo "Downloading extension with cache-bust: $TIMESTAMP"
          
          # Download with cache-busting and no-cache headers
          curl -L -H "Cache-Control: no-cache" -H "Pragma: no-cache" \
            -o extension.zip \
            "https://storage.googleapis.com/avrlgeneration_static_assets_staging/glass_extension_for_testsuite/extension.zip?t=$TIMESTAMP"
          
          unzip extension.zip -d chrome-extension
          
          # Verify manifest exists
          if [ ! -f "chrome-extension/manifest.json" ]; then
            echo "Error: manifest.json not found!"
            exit 1
          fi
          
          # Verify test bridge code exists in content.js
          if grep -q "__avrl-glass-test-bridge__" chrome-extension/content/content.js 2>/dev/null; then
            echo "✓ Test bridge code found in extension"
          else
            echo "⚠ WARNING: Test bridge code NOT found in extension"
            echo "  Tests requiring bridge will be skipped"
          fi
          
          echo "Extension downloaded successfully"
      
      - name: Run tests with downloaded extension
        env:
          EXTENSION_PATH: ${{ github.workspace }}/chrome-extension
          TEMPLATE_FILE_PATH: file://${{ github.workspace }}/template0.html
          GLASS_USERNAME: ${{ secrets.GLASS_USERNAME }}
          GLASS_PASSWORD: ${{ secrets.GLASS_PASSWORD }}
        run: |
          echo "Running tests with intercepted template"
          echo "Template file: $TEMPLATE_FILE_PATH"
          pytest tests/ -v
        continue-on-error: false
      
      # List test results directory contents (for debugging)
      - name: List test results
        if: always()
        run: |
          echo "=== Test Results Directory ==="
          if [ -d "test-results" ]; then
            echo "✓ test-results exists"
            ls -lahR test-results/
          else
            echo "✗ test-results directory not found"
          fi
      
      # Upload screenshots and test artifacts (always run, even on failure)
      - name: Upload test screenshots and artifacts
        if: always()  # Always upload, even if tests fail
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ github.run_number }}
          path: |
            test-results/screenshots/
            test-results/traces/
          retention-days: 30
          if-no-files-found: ignore
      
      # === OPTION 2: Checkout from different repository ===
      # - name: Checkout Chrome Extension
      #   uses: actions/checkout@v4
      #   with:
      #     repository: YOUR-USERNAME/YOUR-EXTENSION-REPO
      #     path: chrome-extension
      #     token: ${{ secrets.GITHUB_TOKEN }}  # or PAT_TOKEN for private
      
      # - name: Run tests
      #   env:
      #     EXTENSION_PATH: ${{ github.workspace }}/chrome-extension
      #   run: |
      #     pytest tests/ -v --html=test-results/report.html --self-contained-html
      
      # === OPTION 3: Extension in same repo ===
      # - name: Run tests with extension from repo
      #   env:
      #     EXTENSION_PATH: ${{ github.workspace }}/my-extension
      #   run: |
      #     pytest tests/ -v --html=test-results/report.html --self-contained-html
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-test-results
          path: |
            test-results/screenshots/
          retention-days: 30
      
      - name: Upload HTML report separately for easy viewing
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: html-test-report
          path: test-results/report.html
          retention-days: 30
      
      - name: Comment PR with test results
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const reportPath = 'test-results/report.html';
            
            let comment = '## 🧪 Test Results\n\n';
            
            if (fs.existsSync(reportPath)) {
              comment += '✅ Tests completed. Download the full report from the artifacts.\n\n';
            } else {
              comment += '❌ Test report not found.\n\n';
            }
            
            comment += `**Commit:** ${context.sha}\n`;
            comment += `**Workflow:** ${context.workflow}\n`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

