[pytest]
# Pytest configuration for Playwright Chrome Extension testing

# Output options
addopts = 
    -v
    --tb=short
    -x
    --maxfail=1

# Timeout for tests (in seconds)
timeout = 500

# Base configuration
testpaths = tests
python_files = test_basic.py
python_functions = test_glass_template

